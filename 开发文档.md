# 在此进行一些技术说明
软件采用网页渲染，内置提示词及四种模式，调用企业级api进行输出，模型优秀可显著降低AI率

软件已对市面上的常见模型做了适配，且可以调动`深度思考`模式

#### 目前已经适配的厂家如下
OpenAI旗下的chatgpt系列模型、Google旗下的Gemini系列模型、阿里旗下的Qwen系列模型、智谱系列模型、Deepseek系列模型、Claude系列模型

如果调用的模型支持深度思考模式，软件的深度思考开关会起作用，如果模型本身不支持深度思考，思考按钮将会失效

## 关于配置接口（引擎设置）
由于文本模型消耗算力巨大，已不可能在本地个人电脑上部署，所以我们需要调用企业级api key进行模型调度，点击右上方`引擎设置`按钮，您需要自备三个参数
### API key
这是你调用AI的唯一凭证，请妥善保管不要泄露给其他人，大多数厂家都可以免费申请一个API key，申请完成后填入这里
### base URL 
请查阅各个厂家的开发文档，寻找适配当前模型的 base URL 地址，建议使用兼容OpenAI格式，例如Gemini的base URL地址且兼容OpenAI格式为`https://generativelanguage.googleapis.com/v1beta/openai/`，大部分厂家其实都兼容OpenAI格式，例如阿里集团的模型，仔细阅读开发文档就可以找到
### 模型名称Modle
请填入你要调用的模型名称，例如`gemini-2.5-pro`,代表将会调用2.5 pro这个版本的模型，具体模型名称也请查阅厂家开发文档

填好三个信息后保存，并且尝试运行一次程序，成功后会在本地根目录生成一个json文件，里面包含了输入的API key等信息，刷新页面，再次点击配置接口，您可以看到往次所调用的模型信息，方便快速切换，**再次提醒：千万不可将自己的API key信息泄露给其他人，发送文件时请注意删除json文件防止泄露密钥！**
## 关于提示词
提示词已在本地储存，放在`prompts.py`文件中
## 关于模式选择
本软件提供四种模式选择
### 学术润色
可以把一段文字的表述学术化
### 逻辑增强
增强话术之间的逻辑性
### 混沌感情
把一段文字的表述情绪化，口语化，此模式并不适合用作学术论文写作
### 深度洗稿
重磅功能，通过两段洗稿大幅降低AI率，实测效果显著，原理是通过先润色后增强，配合提示词彻底去除AI味，由于是两段式输出，消耗的token数量也会大幅增加
## 关于Token消耗
软件会显示本次启动所消耗的Token数量（现在Token也有中文名字啦，叫 `词元` )
token显示分为两部分，一部分是总的消耗，显示在token翻牌显示器中，另一部分是缓存命中token数量，显示在Cached翻牌显示器中，是否会触发缓存命中取决于您使用的模型支不支持缓存，大部分模型触发缓存命中的条件是输入token数量>1024，剩余token数量请登录您的所选模型厂家API官网查询
## 关于模型选择
由于软件的需求只是降低文本的AI率，所以不需要过于复杂的模型，只使用纯文本模型即可，为了进一步节省成本，建议使用轻量化模型，无需使用例如Gemini3.1pro或者Qwen MAX或PLUS版本，另外为了维持改写效果，也不建议使用过于古早的模型或过于轻量的模型

建议使用Gemini2.5 pro、 Qwen3.5-flash等模型，且关闭深度思考，只有极难降低AI率的文本才尝试打开深度思考模式

注意，软件降低AI率不保证100%可靠，但是大部分时间是可靠的，个别几段无法降低AI率可以忽略不计，因为论文AIGC检测是检测整篇文章的AI率，只要把大部分段落的AI率降低即可达成目的
## 关于切分
如果段落过长，软件会自动进行切分，您可以打开切分显示，被切分的段落将会以两种颜色交替高亮显示，注意，无论你是否打开切分显示，自动切分都是强制执行的，因为AI输出过长的文章就不可避免的存在AI味，更容易被AIGC察觉，通过控制段落长度来控制输出长度，达到降低AI味的目的
## 关于时光机
时光机会自动记录近30次改写记录，并且可以回溯界面
## 关于免检
现在可以手动划分免检区了，划分的区域将不会进行改写，会原封不动的保留下来
## 关于段落透视
可以手动关闭不需要改写的段落，也可以划分和取消免检区，可以使用智能侦测屏蔽区按钮，会自动关闭过短的段落，参考文献，和标题等
## 关于API 限流保护
开启后可以调整连续改写的段落数，防止过渡请求导致请求被驳回，可自行设置等待时间
## 关于检测AI
点击后会自动调用DeepVeri进行AI检测，并且输出一份检测报告，使用时请确保DeepVeri处于连线状态
## 关于与DeepVeri进行联机
启动软件后，会自动在5005端口进行寻找，如果也开启了DeepVeri，会自动建立连接,并且每隔3秒向对方发送一个心跳信号，用来刷新连接状态
## 关于智能接力
此按钮会自动把输出文本放入输入框，并且读取各段AI率，AI率小于35%的段落将自动免检，同时，系统也会读取用户之前的设置，如果用户已经手动将某段关闭，那么接力后此段仍然会处于关闭状态，无论AI率有多高
## 关于引擎设置面板的四个参数
temperature—— 控制生成文本的随机程度。取值范围：0到2的浮点数，默认值1.0。该参数通过对 logits 进行缩放来调整下一个 token 的概率分布：1. 当temperature趋近0时，概率分布变得尖锐，模型几乎总是选择最高概率的 token，输出高度确定且重复性极强，适合代码生成、数学解题、事实性问答等需要精确性的任务；2. 当temperature = 1.0时，保持原始概率分布，在合理性与多样性之间取得平衡，是大多数通用对话场景的默认选择；3. 当temperature > 1.0（如1.3~1.8）时，概率分布被拉平，中等概率的 token 获得更多被选中的机会，输出变得多样、有时出人意料，适合创意写作、头脑风暴以及降低 AI 检测率的改写任务；4. 极端值2.0会导致随机性过高，可能产生语法正确但逻辑松散的文本，不建议用于生产环境。注意：temperature不改变模型的事实知识，仅影响表达方式。

top_p—— 核采样（Nucleus Sampling），动态限制候选 token 的选择范围。取值范围：0到1的浮点数，默认值1.0。其工作原理是：1. 将 token 按概率从高到低排序，然后累加概率，直到累加和达到top_p值，模型只从这批 token 中采样，丢弃剩余的低概率 token；2.top_p = 1.0表示不截断，所有 token 都在候选池中；3.top_p = 0.9表示只考虑概率总和占 90% 的那批最可能的 token，排除尾部 10% 的罕见词；4.top_p = 0.5则候选池很小，输出非常保守，容易重复。top_p通常与temperature配合使用：高temperature+ 高top_p（如0.9~0.95）能最大化多样性；低temperature+ 低top_p则极度保守。最佳实践建议：大多数场景下只调整temperature而保持top_p = 1.0，除非需要明确禁止罕见词出现。

frequency_penalty—— 词频惩罚，减少词汇的机械重复。取值范围：-2到2的浮点数，默认值0。其核心机制是：1. 在生成每个新 token 时，模型会检查该 token 在已生成文本中出现的次数，frequency_penalty按照出现次数成比例地减少该 token 的 logit 值，出现越多次惩罚越重；2. 正值（如0.3~0.6）可以有效抑制同一个词的反复出现，迫使模型换用同义词或调整句式，例如将连续的“此外”“此外”“此外”改为“此外”“另外”“同时”；3. 值过高（> 1.0）会导致模型过度回避常用词，甚至不敢重复必要的术语，使文本显得不自然；4. 负值会鼓励重复（几乎不用，仅特殊研究场景）。该参数主要解决句子层面的重复问题，适合用于长文本生成、内容改写以及任何希望减少啰嗦感的场景。

presence_penalty—— 主题新鲜度，鼓励模型引入全新话题。取值范围：-2到2的浮点数，默认值0。其核心机制与frequency_penalty的区别在于：1.presence_penalty的惩罚条件不是“出现次数”，而是“是否至少出现过一次”——只要某个 token 在已生成文本中出现过，无论几次，都会被统一惩罚；2. 正值（如0.3~0.5）会促使模型生成全新的词，从而推动话题切换，避免始终围绕同一观点打转，更像人类聊天的发散思维；3.presence_penalty = 0时模型可以自然深入一个主题，逐层展开；4. 值过高（> 1.0）会导致文本频繁跳跃，缺乏连贯性，适合头脑风暴但不宜用于需要深度论述的场景。举例：介绍“电动汽车优点”时，默认输出会分点罗列；加了presence_penalty后，模型会自然穿插“电池回收”“充电便利性”“驾驶体验”等相关但不同的话题。该参数与frequency_penalty互补——前者管词汇重复，后者管主题粘滞。
### 注意事项
Gemini和Claude等傲娇模型因为对openai格式的兼容性不是很好，所以为了防止报错会自动剔除掉frequency_penalty和presence_penalty两个参数，还是建议使用兼容openai格式的模型